name: budget_buddy_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  http: ^1.3.0
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

  # Dodajemy flutter_launcher_icons:
  flutter_launcher_icons: ^0.13.1

flutter:

  uses-material-design: true

  # Dodajemy assets (upewnij się, że logo jest pod tą ścieżką)
  assets:
    - assets/images/BudgetBuddyLogo.png

flutter_icons:
  android: true
  ios: true
  image_path: "assets/images/BudgetBuddyLogo.png"
